services:
  control-plane:
    build: .
    environment:
      CSG_HTTP_ADDRESS: ":8080"
      CSG_SHUTDOWN_TIMEOUT: "10s"
    ports:
      - "8080:8080"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
